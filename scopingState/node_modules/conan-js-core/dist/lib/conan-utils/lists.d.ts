import { IBiFunction } from "./typesHelper";
export declare class Lists {
    static mergeCombine<FROM, TO_MERGE_COMBINE>(from: FROM[], toMerge: TO_MERGE_COMBINE[], comparator: IBiFunction<FROM, TO_MERGE_COMBINE, boolean>, merger: IBiFunction<FROM, TO_MERGE_COMBINE, TO_MERGE_COMBINE>): TO_MERGE_COMBINE[];
}
