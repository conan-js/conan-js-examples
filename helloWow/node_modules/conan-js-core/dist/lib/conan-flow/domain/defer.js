import { isAsap } from "../../conan-utils/asap";
export function deferParser(deferLike, otherwise) {
    if (!isAsap(deferLike))
        return deferLike;
    return {
        action: deferLike,
        name: otherwise.name,
        payload: otherwise.payload
    };
}
//# sourceMappingURL=defer.js.map