import { BaseRules } from "./baseRules";
class NumberPredicates {
    static isNumber() {
        return (value) => typeof value === "number";
    }
}
export class NumberRules {
    static ifNumber() {
        return BaseRules.if('ifNumber', NumberPredicates.isNumber());
    }
}
//# sourceMappingURL=numberRules.js.map