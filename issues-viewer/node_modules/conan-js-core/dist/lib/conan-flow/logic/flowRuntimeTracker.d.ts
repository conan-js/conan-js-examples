import { FlowRuntimeEvent, FlowRuntimeEventTiming } from "../domain/flowRuntimeEvents";
import { IPartial } from "../..";
import { FlowOrchestrator } from "./flowOrchestrator";
export declare class FlowRuntimeTracker {
    private readonly orchestrator;
    private readonly event;
    constructor(orchestrator: FlowOrchestrator, event: IPartial<FlowRuntimeEvent>);
    trace(timing: FlowRuntimeEventTiming, shortDesc?: any, payload?: any): FlowRuntimeTracker;
    highlight(timing: FlowRuntimeEventTiming, shortDesc?: any, payload?: any): FlowRuntimeTracker;
    private doTick;
}
