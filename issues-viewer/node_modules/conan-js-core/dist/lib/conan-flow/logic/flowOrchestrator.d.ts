import { FlowEvent, FlowEventSource, FlowEventType } from "../domain/flowRuntimeEvents";
import { FlowRuntimeTracker } from "./flowRuntimeTracker";
import { IConsumer } from "../..";
import { FlowImpl } from "./flowImpl";
export declare class FlowOrchestrator {
    private readonly eventsProcessor;
    private currentTracker;
    constructor(eventsProcessor: IConsumer<FlowEvent>[]);
    onRuntimeEvent(tracker: FlowRuntimeTracker, event: FlowEvent): void;
    createRuntimeTracker(flowController: FlowImpl<any, any>, source: FlowEventSource, runtimeEvent: FlowEventType, payload?: any): FlowRuntimeTracker;
}
