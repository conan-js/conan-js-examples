import { Thread } from "../../conan-thread/logic/thread";
import { MonitorFacade } from "../../conan-monitor/domain/monitorFacade";
import { IBiFunction } from "../..";
import { MonitorInfo } from "../../conan-monitor/domain/monitorInfo";
import { PipeThreadDef } from "../domain/pipeThreadDef";
import { ThreadFacade } from "../../conan-thread/domain/threadFacade";
export declare class Pipes {
    static fromMonitor<FROM, INTO>(monitor: MonitorFacade<FROM>, baseValue: INTO, monitorMerger: (IBiFunction<MonitorInfo, INTO, INTO>), dataMerger: IBiFunction<FROM, INTO, INTO>): Thread<INTO>;
    static tupleCombine<LEFT, RIGHT, ACTIONS = void>(left: Thread<LEFT>, right: Thread<RIGHT>, baseValue: [LEFT, RIGHT], pipeThreadDef?: PipeThreadDef<[LEFT, RIGHT], ACTIONS>): ThreadFacade<[LEFT, RIGHT], {}, ACTIONS>;
}
