import * as React from "react";
import { IBiFunction, IConsumer, IReducer } from "../../index";
import { Status } from "../../conan-flow/domain/status";
import { ConanState } from "../conanState";
import { DefaultActions } from "../conan";
import { DefaultActionsFn } from "../../conan-flow/domain/actions";
import { ConanFlow } from "../conanFlow";
export declare function useFlow<USER_STATE = string, STATUSES = any>(flow: ConanFlow<STATUSES>, setState: IConsumer<IReducer<USER_STATE>>, mapper?: IBiFunction<Status<STATUSES>, USER_STATE, USER_STATE>): void;
export declare function useFlowStatus<USER_STATE, STATUSES, STATUS extends keyof STATUSES>(flow: ConanFlow<STATUSES>, status: STATUS, setState: IConsumer<IReducer<USER_STATE>>, mapper?: IBiFunction<STATUSES[STATUS], USER_STATE, USER_STATE>): void;
export declare function useConanState<STATE, ACTIONS = DefaultActionsFn<STATE>>(state: ConanState<STATE, ACTIONS>, fallbackValue?: STATE): [STATE, ACTIONS, React.ComponentType];
export declare function useContextConanState<STATE, ACTIONS = DefaultActions<STATE>>(): [STATE, ACTIONS];
