import { IConsumer } from "../..";
import { Asap } from "../../conan-utils/asap";
export declare type DeferFn<MUTATORS> = Asap<IConsumer<MUTATORS>>;
export interface Defer<MUTATORS> {
    action: DeferFn<MUTATORS>;
    payload: any;
    name?: string;
}
export declare type DeferLike<MUTATORS> = Defer<MUTATORS> | DeferFn<MUTATORS>;
export interface Otherwise {
    name: string;
    payload: any;
}
export declare function deferParser<MUTATORS>(deferLike: DeferLike<MUTATORS>, otherwise: Otherwise): Defer<MUTATORS>;
